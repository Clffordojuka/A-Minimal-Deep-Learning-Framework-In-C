# Dataset
dataset_path = raw/data/housing.csv
num_samples = 20640
num_features = 8
train_ratio = 0.8

# Architecture
hidden_layers = 32,16

# Training
epochs = 20
batch_size = 32
learning_rate = 0.0005
l2_lambda = 0.0001
early_stopping_patience = 5

# Output files
checkpoint_path = models/best_housing_model.bin
history_path = processed/training_history.csv
model_path = models/housing_model.bin
stats_path = models/housing_stats.bin